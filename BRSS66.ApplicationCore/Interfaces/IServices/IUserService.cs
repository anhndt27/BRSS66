using BRSS66.ApplicationCore.Models;

namespace BRSS66.ApplicationCore.Interfaces.IServices;

public interface IUserService
{
    /*Task<PagedResponse<UserInformation>> GetUsers(int currentPage, int pageSize, string? sortColumnDirection, string? sortColumnName, string? textSearch);
    Task<UserInformation> GetUserById(int id);
    Task CreateUser(UserInformation model);
    Task UpdateUser(int id, UserInformation model);
    Task ActiveUser(int id);
    Task InactiveUser(int id);*/
}